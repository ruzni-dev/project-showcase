Merged site (NexusForge + NexusTech)

How to preview locally:

1. Open `merged-site/index.html` in your browser.
2. Or serve the folder with a local static server, for example:

```bash
# Python 3
python -m http.server 8000
# then open http://localhost:8000/merged-site/
```

What I created:
- `index.html`, `about.html`, `services.html`, `portfolio.html`, `ai-solutions.html`, `contact.html`
- `css/theme.css` — merged theme extracted from Deepseek
- `js/main.js` — interactive behaviors (AOS init, navbar, chat, contact form)

Next suggested steps:
- Replace placeholder content and images with your real assets
- Merge additional styles from `it-solutions-website/css/custom.css` as needed
- Test responsiveness and accessibility

Images/assets note:
- The merged pages reference two images from the original site located at `it-solutions-website/images/hero_illustration.png` and `it-solutions-website/images/about_tech_team.png`.
- If you want a standalone `merged-site` folder, copy those image files into `merged-site/images/` and update the image paths (or keep the current relative paths if both folders remain in the same parent).
