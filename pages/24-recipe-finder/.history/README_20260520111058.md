# 24-recipe-finder

## Project Structure

```text
24-recipe-finder/
├── index.html
├── assets/
│   ├── css/
│   │   └── style.css
│   └── js/
│       └── script.js
└── README.md
```

## Notes

The app is a single-page static site, so the structure keeps the entrypoint at the root and groups presentation and behavior under `assets/`.